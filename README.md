
  # tipografia

  This is a code bundle for tipografia. The original project is available at https://www.figma.com/design/CzPA9xcOtiCq4jux9JSif7/tipografia.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  